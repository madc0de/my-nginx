server {
        listen 80 default_server;
        server_name localhost;

        location / {
                 return http://$host/welcome
        }

        location /welcome {
                 root /hostdata/default;
                 index index.html;
        }
}
